# Asst_1_RotatingBunny_Win

Download all files in "starter code" folder

Create a new empty project, add all source files and resource files (objects and shaders), add the property sheet (OpenGL and NanoGUI), make sure you can build and run the starter code (see the bunny, ground plane, coordinate system)

Either modify Animation or Renderer class (.cpp & .h files) to rotate the bunny 
